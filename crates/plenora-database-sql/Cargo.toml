[package]
name = "plenora-database-sql"
description = "AST SQL tipizzato e renderer redatto multi-dialect"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license-file.workspace = true

[dependencies]
plenora-database-core.workspace = true
serde.workspace = true

# Serve solo al benchmark offline `bench_sql_render`, che emette JSONL.
[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
